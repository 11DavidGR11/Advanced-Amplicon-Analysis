# MultiAmplicon Integrator guide

Use this application to combine two or more non-negative integer count tables. Load the files, inspect header alignment, select the count columns, decide whether duplicate identifier/taxonomy rows should be aggregated, and export the integrated table as TSV. Equivalent headers are aligned by name rather than position. Do not combine tables representing incompatible taxonomic markers without documenting the biological limitations.
